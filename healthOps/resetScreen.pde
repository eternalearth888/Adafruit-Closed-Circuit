class resetScreen{
  
  void drawReset(){
    fill(0);
    hr.drawHeart();
    textSize(60);
    textAlign(CENTER);
    String string1 = "Please reset all pieces to restart";
    text(string1, width/2, height/2);
  }
  
   //Wait until all connections formed then go to opening screen s1
}
